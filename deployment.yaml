apiVersion: apps/v1
kind: Deployment
metadata:
  name: test
  labels:
    app: hello-node
spec:
  selector:
    matchLabels:
      app: hello-node
  template:
    metadata:
      labels:
        app: hello-node
    spec:
      containers:
        - ports:
            - containerPort: 8080
          image: registry.k8s.io/e2e-test-images/agnhost:2.39
          command:
            - /agnhost
            - netexec
          name: hello-node
  replicas: 3
